<Window x:Class="MilitaryShooter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MilitaryShooter"
        mc:Ignorable="d"
        Title="MainWindow">
    <Grid>
        <Viewbox Stretch="Fill">
            <Canvas Name="GameCanvas" Background="Black" Focusable="True" Height="900" Width="1600" MouseMove="MouseMoveHandler" MouseLeftButtonDown="GameCanvas_MouseLeftButtonDown" MouseLeftButtonUp="GameCanvas_MouseLeftButtonUp" KeyDown="GameCanvas_KeyDown" KeyUp="GameCanvas_KeyUp">
                <Label Name="ScoreText" Content="Score: 0" FontSize="18" FontWeight="Bold" Foreground="White" Canvas.Left="0" Margin="20,10,0,0" />
                <Label Name="Damage" Content="Damage: 0" FontSize="18" FontWeight="Bold" Foreground="White" Canvas.Right="0" Margin="0,10,20,0" />
            </Canvas>
        </Viewbox>
        <Grid x:Name="GameOverMenu"
              Background="#CC000000"
              Grid.RowSpan="2"
              Grid.ColumnSpan="3"
              Visibility="Hidden">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <TextBlock Text="Game Over"
                           FontSize="48"
                           TextAlignment="Center"
                           Foreground="White"
                           Margin="48" />
                <TextBlock x:Name="FinalScoreText"
                           Text="Score: "
                           FontSize="36"
                           Foreground="White"
                           TextAlignment="Center"
                           Margin="36" />
                <Button Content="Play Again"
                        Foreground="White"
                        Background="DimGray"
                        Margin="0,20,0,0"
                        Padding="5"
                        FontSize="24"
                        MaxWidth="200"
                        Click="PlayAgain_Click" />
                <Button Content="Quit"
                        Foreground="White"
                        Background="DimGray"
                        Margin="0,20,0,0"
                        Padding="5"
                        FontSize="24"
                        MaxWidth="200"
                        Click="Quit_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>